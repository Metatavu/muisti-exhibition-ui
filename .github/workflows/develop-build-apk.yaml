name: Build APK (develop)
on:
  push:
    branches:
      - develop
jobs:
  build:
    runs-on: ubuntu-latest
    environment: master
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: true
          fetch-depth: 0

      - name: Envs
        run: |
          mkdir -p envs
          echo "SIGN_KEYSTORE": "${{ secrets.SIGN_KEYSTORE }}", >> envs/envs.txt
          echo "SIGN_STORE_PASSWORD": "${{ secrets.SIGN_STORE_PASSWORD }}", >> envs/envs.txt
          echo "SIGN_KEY_ALIAS": "${{ secrets.SIGN_KEY_ALIAS }}", >> envs/envs.txt
          echo "SIGN_KEY_PASSWORD": "${{ secrets.SIGN_KEY_PASSWORD }}", >> envs/envs.txt
          echo "MUISTI_API_BASE_URL": "${{ secrets.MUISTI_API_BASE_URL }}", >> envs/envs.txt
          echo "KEYCLOAK_URL": "${{ secrets.KEYCLOAK_URL }}", >> envs/envs.txt
          echo "MQTT_BASE_TOPIC": "${{ secrets.MQTT_BASE_TOPIC }}", >> envs/envs.txt
          echo "KEYCLOAK_REALM": "${{ secrets.KEYCLOAK_REALM }}", >> envs/envs.txt
          echo "KEYCLOAK_CLIENT_ID": "${{ secrets.KEYCLOAK_CLIENT_ID }}", >> envs/envs.txt
          echo "KEYCLOAK_USERNAME": "${{ secrets.KEYCLOAK_USERNAME }}", >> envs/envs.txt
          echo "KEYCLOAK_PASSWORD": "${{ secrets.KEYCLOAK_PASSWORD }}", >> envs/envs.txt
          echo "KEYCLOAK_DEMO_TAG": "${{ secrets.KEYCLOAK_DEMO_TAG }}", >> envs/envs.txt
          echo "DESTRUCTIVE_MIGRATIONS": "${{ secrets.DESTRUCTIVE_MIGRATIONS }}", >> envs/envs.txt
          echo "MQTT_URLS": "${{ secrets.MQTT_URLS }}", >> envs/envs.txt
          echo "MQTT_PASSWORD": "${{ secrets.MQTT_PASSWORD }}", >> envs/envs.txt
          echo "MQTT_USER": "${{ secrets.MQTT_USER }}", >> envs/envs.txt
          echo "MAP_BOX_ACCESS_TOKEN": "${{ secrets.MAP_BOX_ACCESS_TOKEN }}", >> envs/envs.txt
          echo "UPDATE_URL": "${{ secrets.UPDATE_URL }}", >> envs/envs.txt
          echo "UPDATES_BUCKET": "${{ secrets.UPDATES_BUCKET }}", >> envs/envs.txt
          echo "AWS_ACCESS_KEY_ID": "${{ secrets.AWS_ACCESS_KEY_ID }}", >> envs/envs.txt
          echo "AWS_SECRET_ACCESS_KEY": "${{ secrets.AWS_SECRET_ACCESS_KEY }}", >> envs/envs.txt

      - name: Upload APK
        uses: shallwefootball/s3-upload-action@master
        with:
          aws_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_bucket: ${{ secrets.UPDATES_BUCKET }}
          source_dir: envs
          destination_dir: ""